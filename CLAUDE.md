# 家族ボード / family-board — 作業メモ

家族5人の予定を人ごとの縦レーンで常時表示するPWA。
古いiPhone → HDMI → モバイルモニターにミラーリングして壁掛け表示する用途。

## 前提・制約

- **TimeTreeからのデータ取得は不可能**。Connect App（API）は2023-12-22終了、
  外部カレンダー連携は「取り込む」一方通行で書き出し機能がない。
  → Googleカレンダーを正のデータ元とし、TimeTreeは表示側に回す構成に決定（ユーザー合意済み）。
- 表示端末は **iPhone 8〜11世代 / iOS 15〜16 想定**。
  `:has()` / container query / CSS nesting / `color-mix()` は **使わないこと**。
  JSも ES2020 相当（`structuredClone` `Array.at` `findLast` 不使用）。
- サイズはすべて `vh` 基準。ミラーリングで拡大されても比率が崩れないため。
  px固定はしない。

## 構成

```
Googleカレンダー → gas/Code.gs（Apps Script Web App, JSON） → app.js（fetch）
```

- **GASのURLはリポジトリに書かない。** 端末の localStorage（`fb.cfg`）にのみ保存する。
  公開リポジトリでも予定が漏れないようにするための設計判断。
- 予定の担当判定の優先順位：タイトルの目印タグ > 予定の色 > カレンダー > `shared`（みんな）
- メンバーの `key`（`father` `mother` `son1` `son2` `daughter` `shared`）は
  `app.js` の `MEMBERS` と `gas/Code.gs` の `CONFIG.members` で **必ず一致** させる。

## 更新時の手順

1. ファイルを編集
2. **`sw.js` の `VER` をインクリメント**（しないとキャッシュが切り替わらない）
3. ローカル確認：Claude Code の preview で `family-board`（http://localhost:8766）
4. 実機相当の確認は 812×375（iPhone横）と 1624×750（モニター相当）の両方で見る

## 動作確認のしかた

- ⚙ の取得URLを空にすると `demoEvents()` のサンプルが出る
- `sample-data.json` はGASが返すJSONの形式サンプル。
  ⚙ に `http://localhost:8766/sample-data.json` を入れれば取得経路のテストになる

## 天気

- Open-Meteo（APIキー不要・CORS対応・無料）。予報と、地名→座標のジオコーディングの2本立て。
- **ジオコーディングは候補を必ず選ばせること。** 先頭を自動採用すると事故る
  （「松本」の第1候補は長野県ではなく沖縄県、「横浜」は青森県が先に出る）。
- 地点はリポジトリに持たず localStorage（`fb.cfg.place`）にだけ保存する。

## 色の決め方

現行は 父=#8FA0B5(灰) 母=#FF7AA8(桃) 長男=#4FA3FF(青) 次男=#4ED9A4(緑) 長女=#FFD84D(黄) みんな=#B98BFF(紫)。
すべてユーザーの明示指定。経緯は「長男=青・次男=緑・長女=黄・母は据え置き」→
父は一度 #FF6E40(朱) にしたが、その後ユーザー指定で「父=灰・みんな=紫」に入れ替えた。
勝手に色相を最適化し直さないこと。変更依頼があった色だけを変える。

## 表示モード（2026-09-04 追加）

壁掛け(横向き・常時表示)と、各自のiPhoneで持って見る用途(縦向き)とで、
必要な画面が違うため3モードに分けた。`renderAll()` が分岐する。

1. **1日タイムライン**（横向き・既定）: `renderBoard()`。壁掛け表示用の横並びレーン。
2. **アジェンダ**（縦向き・既定）: `renderAgenda()`。`isPortrait()` で自動判定。
   横並びレーンを狭い画面に押し込むと文字が読めなくなるため、
   メンバーごとに見出し→予定を縦に並べる一覧形式にした。
3. **月間ビュー**: `renderMonthView()`。`STATE.monthMode` で横縦どちらでも切替可能。
   ⚙横の「月」ボタン、または月表示中の‹/›は月移動になる。日付タップで1日表示に戻る。

**ハマったバグ:** `.board` に `min-height: 0` が無く、flexboxの既定
(`min-height:auto`)でアジェンダの中身の高さぶん親要素ごと膨張し、
画面からはみ出た分が `body{overflow:hidden}` で静かに消えていた
（スクロールバーも出ない）。`.agenda` 自体に `overflow-y:auto` を
付けても、親がちゃんと高さを制限してくれないと効かない。
flex列の子要素は明示的に `min-height:0` を付けること。

**月間ビューのための変更:** 予定取得の日数を `days=45` → `days=90` に拡張
（GAS側の上限は120日）。45日だと月間ビューで来月後半が空になるため。

## 月間ビューの幅対応 / ボタンの最小タップサイズ（2026-09-04 追加）

- 月間ビューは `isNarrowScreen()`(`window.innerWidth < 820`)で判定し、
  幅が狭い(≒手元のiPhone)ときは時刻を省いてタイトルだけのピルにする。
  文字サイズは実測で調整済み(`.mc.narrow .mc-ev` font-size:8px)：
  390px幅・7列(1列あたり実測44px)で「家族で映画」(5文字)が省略されずに
  収まることをDOMの scrollWidth>clientWidth で確認している。
  最初はドット表示(メンバー色の丸だけ)にしたが、ユーザーから
  「文字を読みたい、時間は要らないので5文字入る大きさに」と要望があり、
  タイトルのピル表示に戻した経緯がある。日付タップで詳細(アジェンダ)へ。
  幅が広い(≒壁掛けモニター)ときは今まで通り時刻付きのピルのまま。
  この閾値([style.css](style.css) `.mc.narrow`)はiPhoneの画面幅を前提に
  実測したものなので、フォントやレイアウトを変えたら測り直すこと。
- `orientation`ではなく実際の**幅**(`innerWidth`)で判定している。
  同じiPhoneでも「HDMIミラーリングで壁掛け」と「手に持って横向き」は
  向きの上では同じ`landscape`だが、後者は物理的に幅が狭いままなので
  向きだけでは区別できない。
- `.icon-btn` に `min-width/min-height: 44px` と `font-size/padding` の
  px下限(`max(vh値, px値)`)を追加。壁掛けミラーリング前提の`vh`基準サイズは、
  同じiPhoneを手に持って直接操作すると物理的に小さすぎてタップしづらいため。
- **副作用に注意:** ボタンを大きくした結果、狭い画面で `.board-top` の
  見出し(日付+補足テキスト)が実質幅ゼロまで押し潰され、補足テキストが
  一文字ずつ縦に折り返す崩れ方をした。`.board-top`/`.board-title` に
  `flex-wrap: wrap` を追加して、収まらないときはツール行が下に折り返す
  ようにして解消。ヘッダー領域を固定幅化する変更をするときは、
  必ず一番狭い実測幅(390px)で確認すること。

## iOSホーム画面アプリ：設定モーダルが2回目開けない不具合（2026-09-04 修正）

**症状:** iPhoneでホーム画面に追加したアプリで、⚙設定を一度開いて閉じると、
2回目以降タップしても開かなくなる。

**原因:** `.modal`（画面全体を覆う `position:fixed` の背景）自体に
`overflow:auto` と `-webkit-overflow-scrolling:touch` が付いていた。
これは iOS の standalone/fullscreen Webアプリで既知の不具合パターンで、
「スクロール可能な fixed 要素を `[hidden]`/`display:none` で出し入れすると、
以後タッチが一切反応しなくなる」ことがある。中身の `.modal-box` 側には
別途ちゃんとスクロールが付いていたので、外側の背景にまで付けていたのは
不要だった。

**修正:**
- `.modal`（背景）からスクロール関連プロパティを削除、`.modal-box`（中身）
  側にだけ残す（[style.css](style.css) 内 `.modal` / `.modal-box`）
- 開閉処理を `closeSettings()` に一本化し、フォーカス解除・スクロール位置
  リセットを必ず行うようにした（防御的な保険。本命の原因は上記のCSS）
- `black-translucent` ステータスバー用に `.side` / `.board` へ
  `env(safe-area-inset-top)` を追加（ノッチ/ステータスバー下に
  要素が隠れて反応しない、という別要因も同時に潰した）

**検証の限界:** 実機・iOSシミュレータ(Xcode未インストールのため利用不可)
での再現確認はできていない。Chromiumベースのブラウザでは再現しない
WebKit固有の挙動のため。CSSプロパティの反映と、開閉ロジックが
リグレッションしていないことは確認済み。ユーザーに実機での確認を依頼する。

## Service Worker の注意（ハマりどころ）

- キャッシュ削除は **`family-board-` プレフィックスのものだけ**。
  Cache Storage はオリジン単位で共有されるので、同じ `yasuokun-pro.github.io` にある
  ヘリナビの `hnav-tiles`（オフライン地図タイル）を消してしまう。
- 逆方向は未対応。ヘリナビ側 `sw.js` も同様のプレフィックスガードが必要
  （ユーザー未承認のため未着手）。
- 取得は `fetch(url, {cache:'no-cache'})`。これがないと GitHub Pages の
  `max-age=600` により、VERを上げても最大10分間古いファイルが出続ける。

## 終日予定とタイムゾーン

- GASは終日予定を **日付のみ**（`YYYY-MM-DD`）で返す。`end` は排他的（翌日）。
- クライアントの `parseWhen()` が日付のみをローカル0時として解釈する。
  `new Date("2026-09-06")` はUTC0時＝JST朝9時になるため、素で渡してはいけない。
- これは「ファミリー カレンダー」のタイムゾーンがUTCのままでも壊れないようにするための対策。
  時刻を持たせると 9/6 09:00→9/7 09:00 と解釈され、ボード上で2日にまたがって表示される（実測確認済み）。

## カレンダー構成（2026-09-04 にユーザーと決めた方針）

- 予定の置き場は「ファミリー カレンダー」１本。全員分を `【父】` などの目印付きで入れる。
- 個人カレンダー（ユーザー本人のGmailアドレス）は**そのまま読ませない**。
  Gmailが受信メールから宿・レストランの予約を自動生成するため、
  壁掛けボードに出すと来客に見られる。実際に宿泊予約8件・飲食店1件が入っていた。
- サッカーの予定（クラブサイトから個人カレンダーに自動取り込みされていたもの11件）は
  ユーザーがCowork経由でファミリーカレンダーへ**移動済み**（2026-09-04）。
  よって `CONFIG.tagOnlyCalendars` は使わず `[]` のまま。個人カレンダーは
  今後の取り込み先自体をファミリーカレンダーに変更する想定。
- `【2年生】` = 長男。`son1` の `tags` に `'2年生'` を追加済み。
- ユーザーはGoogleカレンダー初心者。「片方に入れれば両方に反映されるのか」という
  質問が出た＝予定は1カレンダーに1つだけ属する、という前提から説明が必要。

## ウィジェットタップがデモ表示になる不具合（2026-09-04 修正）

**症状:** Scriptableウィジェットをタップすると、Safariで家族ボードが開くが
デモ表示になっている（本番の予定が出ない）。

**原因:** iOSでは「ホーム画面に追加したアプリ版」と「Safariで開いたタブ」は
同じURLでも**localStorageが別**。⚙で設定した取得URLはアプリ版のlocalStorage
にしか無いため、ウィジェットのタップでSafariが新規に開くと真っさらな状態になる。

**修正:** [app.js](app.js) の起動時に `?endpoint=...` というクエリパラメータを
見て、あれば `CFG.endpoint` に取り込んで保存するようにした（取り込んだら
`history.replaceState` でURLからは消す）。[scriptable/family-board-widget.js](scriptable/family-board-widget.js)
の `widget.url` は、この形式でURLを組み立ててタップ時に渡す。
これでSafari側のlocalStorageが空でも、その場で正しい取得URLが設定される。

## ツールバー(‹今日›月＋⚙)が画面からはみ出す（2026-09-05 修正）

**症状:** 縦向きの実機で、右端の⚙が画面外にはみ出して押せない。
ユーザーのスクリーンショットで実際に確認できた(初めて実機の
スクリーンショットが手に入ったケース)。

**原因:** これまでのローカル検証は幅390px(iPhone 12以降相当)でしか
していなかった。このプロジェクトが元々対象にしている「iPhone 8〜11世代」
には**幅375px**の機種(iPhone 8, SE)が含まれており、そこでは
「読み込み中…/更新HH:MM」のステータス文字列 + ボタン6個(‹今日›月＋⚙、
各min-width:44px)が1行に収まらず、`.board-tools`が`flex-wrap`していなかった
ため右にはみ出していた。375pxで再現できた。

**修正:**
- `.board-tools` に `flex-wrap: wrap` を追加(保険。収まらなければ折り返す)
- 縦向き(`@media (orientation: portrait)`)では `.status`
  (読み込み中・更新時刻の表示)を非表示にして幅を確保。
  壁掛け(常に横向き)には影響しない。
- 375px幅で実際に6ボタンが1行に収まることを確認した。

**教訓:** 「iPhone 8〜11世代」を対象にすると謳っている以上、
検証は必ず375px(iPhone 8/SE幅)でも行うこと。390pxはiPhone 12以降で、
対象範囲の外側。

## 予定追加モーダル：誰を押しても「父」になる／ピルが大きい（2026-09-05 修正）

**症状:** 実機iPhoneで「＋」から予定を追加しようとすると、誰のピルを
タップしても常に「父」が選ばれる。ピル自体も大きくて画面をはみ出す感じがする。

**このMacでは再現しなかった。** Chromiumの通常のクリックでは
`renderAddMemberPicker`の個別リスナー方式でも正しいメンバーが選べており、
再現できないまま2つの対策を打った。

**対策1（クリック判定）:** ピル1つ1つに `addEventListener` を付ける方式から、
親要素 `#ae-members` 1つで受け止めて `event.target.closest('.am-pick')` で
実際に押された要素を特定する「イベント委譲」方式に変更（`initAddMemberPicker()`）。
個別リスナー方式でも理屈の上ではバグらないはずだが、委譲方式の方が
取りこぼし・誤検知の余地が構造的に少ない。

**対策2（サイズ）:** `.am-pick` を `vh` 基準([style.css](style.css))から
`px` 固定（font-size:13px, padding:6px 12px）に変更。`.icon-btn` で
一度ハマったのと同じ理由（壁掛けミラーリング前提のvhサイズは、
同じiPhoneを手に持つと物理的に大きすぎる）。

**テスト中に踏んだ罠:** ローカル検証中、`style.css`を編集して
`location.reload()`しても、`document.styleSheets`が古い内容
（`1.9vh`のまま）を返し続ける現象に遭遇した。fetch()で生のCSSを
取得すると新しい内容(`13px`)なのに、実際に適用されるルールは
古いまま——これは本番のService Workerとは無関係で、**ローカルの
Python簡易サーバー(`http.server`)がキャッシュ制御ヘッダーを
一切付けないため、ブラウザの通常HTTPキャッシュが古いCSSを
使い回していた**のが原因。`navigate()`で明示的にページ遷移させると
直った。実機での「大きすぎる」報告も、同種のキャッシュ残留
（本番側は`cache:'no-cache'`対策済みのはずだが、可能性はゼロではない）
だった可能性がある。

**検証の限界:** 実機での再現はできていない。上記2点は
「最も筋の良い推測に基づく防御的な修正」であり、確定した
root cause の特定はできていない。ユーザーに実機での再確認を依頼する。

## GASデプロイに新しいOAuthスコープが反映されない不具合（2026-09-05）

**症状:** appsscript.json の oauthScopes に書き込み権限
(`calendar.events`) を追加し、既存デプロイを「デプロイを管理」→
バージョンを「新バージョン」にして更新しても、そのデプロイのURLへ
doPost すると毎回 `Specified permissions are not sufficient` エラーになる。
Googleアカウントの「リンク済みアプリ」画面では新しいスコープも
正しく許可済みと表示されるのに、Webアプリ実行時にだけ反映されない。
4時間ポーリングしても直らなかった（=単なる伝播遅延ではない）。

**原因（推定）:** 既存デプロイの「新バージョン」更新では、Webアプリの
実行アイデンティティに紐づくOAuthトークンのスコープ集合が更新されない。
エディタでの直接実行(`実行`ボタン)は毎回フレッシュに評価されるため
すぐ動くが、デプロイされたWebアプリ側は古いスコープのトークンを
使い続けてしまうように見える。

**直し方:** 「デプロイ」→「**新しいデプロイ**」（「デプロイを管理」から
既存を更新するのではない、全く新規のデプロイ）を作ると直った。
ただし**これはURLが変わる**。ボードの⚙・Scriptableウィジェットの設定を
両方とも新URLに更新する必要がある。

**教訓:** oauthScopesを追加/変更したら、既存デプロイの更新では済ませず
「新しいデプロイ」を作ることを最初から想定しておく。旧デプロイのURLは
そのまま生き続ける（読み取りは動く）ので、慌てて片方だけ触らず、
新URLでの動作確認が取れてから、全クライアントの設定を切り替える。

## Scriptableウィジェット（2026-09-04 追加）

- `scriptable/family-board-widget.js`：自分のiPhoneのホーム画面ウィジェット用。
  iOSのWidgetKitはWebページを埋め込めない(WebView禁止)ため、家族ボードの
  PWA本体とは別に、同じGAS JSONエンドポイントを叩いてJavaScriptで
  描き直す構成にした。母艦はPWA、ウィジェットは軽量な別クライアント。
- Xcode/開発者登録なしで作れる「Scriptable」アプリ(無料)向け。取得URLは
  Scriptable側の `Keychain` に保存する(このリポジトリには書かない、という
  PWA本体と同じ設計方針を踏襲)。
- **実機・シミュレータでの動作確認はできていない**（このMacにフルXcode構成が
  無くiOS Simulatorが使えず、Scriptable自体もこのマシンでは動かせないため）。
  構文チェックは `osascript -l JavaScript`(JavaScriptCore、Scriptableと同じ
  エンジン系統)で通した。Scriptable固有API(`WidgetStack.size`で列幅を
  揃える等)は資料に基づく実装だが未検証。ユーザーに実機での確認を依頼する。
- 終日予定の日付のみ文字列パース(`parseWhen`)、失敗時に前回データへ
  フォールバックする設計は、意図的にPWA本体([app.js](app.js))と揃えてある。

## 予定追加モーダル：選択が効かない不具合の真因、編集・削除・メモ機能（2026-09-05）

**症状（続報）:** ボタンサイズ修正後もユーザーの実機で「誰を押しても選択できない」
（父のピルが常に選択状態に見える）が再現し続けた。イベント委譲方式への変更
（前セクション）では直らなかったことをユーザーが実機スクリーンショット付きで報告。

**真因:** `#ae-members`（メンバーピル）と `#s-place-results`（天気の地名候補）が
どちらも `<label class="fld">` で囲われていた。`<label>` は本来ラベル付き
フォーム要素を1個だけ包む要素で、中に複数の `<button>` を入れるのは無効な
マークアップ。WebKit（iOS Safari）はこれを「タップ位置に関わらず、
ラベル内で最初に出てくるラベル付け可能要素にクリックを転送する」という
挙動で解釈するため、どのピルを押しても常に1番目（父）が選ばれていた。
Chromiumはこの挙動を再現しないため、このMac上では終始再現できなかった。

**教訓:** 「委譲方式にしたのに直らない」＝リスナーの実装ではなく、
そもそもイベントがWebKitによって別要素に転送されている可能性を疑うこと。
再現しないバグを追うときは、まずマークアップの妥当性（`<label>` が
複数のクリック可能要素を包んでいないか等）を先に洗うべきだった。

**修正:** [index.html](index.html) の `#ae-members` と `#s-place-results` を
囲っていた `<label class="fld">` を `<div class="fld">` に変更（見出しの
`<span>` はそのまま残す）。[style.css](style.css) 側は `.fld > span` だけでなく
`.fld > label > span`（タイトル等、他のフィールドは引き続き `<label>` のまま）
にも見出しスタイルを当たるようセレクタを追加した。

**追加機能（同時対応、ユーザー要望）:**
- **編集:** アジェンダの予定行をタップすると同じモーダルが編集モードで開く
  （`openEditEvent()`）。見出し・ボタン文言が「追加」→「編集」に変わる。
  保存時は `action:'update'` を送り、GAS側は対象を削除してから
  新規作成し直す（Googleカレンダー側にネイティブな更新APIがない代わりの実装）。
- **削除:** 編集モード時のみ「削除する」ボタンを表示。`window.confirm()`
  で確認してから `action:'delete'` を送信。
- **メモ:** `ae-memo`（textarea）を追加。カレンダー予定の `description` に
  保存・表示する。`pull()` のレスポンスにも `description` を追加。
- **GAS側:** `doPost` を `action`（無指定=作成 / `update` / `delete`）で
  分岐。`findRawEvent(compositeId, calId)` で複合ID
  （`生ID@開始時刻ミリ秒`）から生IDを取り出す際、**生IDの末尾ではなく
  最後の `@` で分割する**必要がある点に注意（Googleの生イベントID自体が
  `xxxx@google.com` のように `@` を含むため、最初の `@` で割ると壊れる）。
  `calId` も往復させて対象カレンダーを一意に特定する。
- **検証:** curl（`--data` + `-L`）で本番URLに対し
  作成→編集→内容確認→削除→消えたことの確認まで一通り実施し、
  実際のGoogleカレンダー（ファミリー カレンダー）に対して動作することを確認済み。
  テスト用イベントは確認後に削除済み。
- **未検証:** 実機iOSでのタップ操作そのもの（`<label>` 修正が実際に
  選択不能を解消するか）はユーザーによる確認待ち。

## 常時表示モード／画面が朝には暗くなっている件（2026-09-10）

**症状:** 数日使ってみたところ、朝起きると画面が暗くなっている。
iPhone本体の「自動ロック→なし」は設定済みなのに発生する。

**このMacでは実機を持たないため確定原因の特定はできない。** ただし
「自動ロック：なし」にしていても画面が暗くなる/消えるケースとして
知られているのは概ね次の3つで、ユーザーに切り分けをお願いした：

1. **バッテリー切れによるシャットダウン** — 画面点灯し続け＋HDMI出力は
   消費電力が大きい。HDMI変換アダプタやモニター側のUSBポートから給電して
   いる場合、供給が細くて夜間に少しずつ放電し、朝には電池切れで完全に
   電源が落ちていることがある。この場合は電源ボタンを押すとApple ロゴの
   起動画面から始まる。→ **iPhone純正の電源アダプタでコンセント給電**に
   しているか確認をお願いした。
2. **ソフトウェアアップデートの自動インストールによる再起動** —
   深夜に自動更新→再起動がかかると、ロック画面まで戻ってしまう
   （自動ロック設定はロック画面自体には無関係）。
   → 設定→一般→ソフトウェアアップデート→自動アップデートをオフに推奨。
3. **発熱によるシステム保護** — 画面点灯し続け＋HDMI出力＋常時充電で
   発熱しやすく、iOSの熱対策で強制的に画面/システムが落ちることがある。
   古いiPhoneはバッテリー劣化もあり充電時の発熱が起きやすい。

**切り分け方法をユーザーに案内:** 朝、電源ボタンを押したときに
①Appleロゴから起動する（→電池切れ/再起動系）か、②すぐに家族ボードが
表示される（→単に画面が暗いだけ＝Wake Lockやスリープ抑止の失敗）かで
原因の系統が変わる。

**コード側で入れた保険（根本原因が①②いずれであっても無害な改善）:**
[app.js](app.js) の `keepAwake()` を強化した。
- Wake Lock APIは **iOS 16.4未満には存在しない**。プロジェクトの対象は
  iOS 15〜16のため、対象機種の一部では元々何もしていなかった可能性がある
  （`if (navigator.wakeLock...)` が静かにスキップされるだけだったため、
  今まで気づけなかった）。
- 対応済みブラウザでも、iOS SafariはWake Lockが無音のうちに解放される
  ことが知られているため、`release`イベントで即再取得、かつ念のため
  3分おきにも再取得を試みるようにした。
- Wake Lock非対応の端末向けに、`canvas.captureStream()`で自作した
  無音動画を再生させ続ける古典的な手法（NoSleep.js等が使うのと同種の
  仕組みだが、ライブラリは使わず自前実装）をフォールバックとして追加。
- **これらはいずれも「バッテリー切れ」「再起動」「熱保護」には無力**。
  そもそも画面点灯を続けたいだけなら自動ロック設定で足りているはずで、
  今回の追加は「JS側のスリープ抑止が実は効いていなかった」場合の保険に過ぎない。
  根本原因が①〜③のどれかは実機での確認待ち。

**常時表示モード（ユーザー要望、あわせて実装）:**
壁掛け中は⚙や＋などのボタンが常時見えている必要がないため、⚙の
「常時表示モード」をONにすると `.board-tools`（ステータス文字＋ボタン一式）
を透明・クリック不可にして隠す。画面のどこかをタップすると6秒間だけ表示され、
また触らなければ自動的に消える（[app.js](app.js) `showKioskControls()` /
`applyKioskMode()`、[style.css](style.css) `body.kiosk-mode`）。
既存の「操作を検知したら`STATE.lastTouch`を更新する」仕組み（日付移動の
5分アイドル復帰などで使っている）にそのまま相乗りしている。

## TimeTree経由の予定が編集できない不具合（2026-09-12 修正）

**症状:** TimeTreeからファミリーカレンダーにコピーした予定（目印タグが
無いため「みんな」レーンに入る）を、ボードの編集画面で別の担当者に
変更して更新しようとすると「予定が見つかりません（既に削除された
可能性があります）」と出て更新できない。この振り分け自体（タグが
無いので「みんな」になる）はタイムツリー側の話で正しい仕様であり、
問題は「その後の編集」が効かないこと。

**原因（実際にユーザーの本番カレンダーの該当予定で確認・特定できた）:**
これらの予定のIDを調べると、Googleカレンダー純正のイベント（例:
`f40b8om2o16vu10fq7pihc0ta8`）とは全く違う、`429DA7E7-FC13-4FCC-96FA-EFE236260B8D`
のようなハイフン区切りの大文字16進数（iCalUID形式）になっていた。
これはTimeTreeなど外部クライアントがGoogleカレンダーAPI経由で
予定を作成したときに、内部リソースIDの代わりにこの形式のIDが
`getId()`で返ってくるケース。**この形式のIDは`CalendarApp.getEventById()`
に渡しても見つからない**という、Apps Script/Calendar APIの既知の制限
（`getEvents()`の一覧には出てくるのに、同じIDで`getEventById()`すると
nullが返る）に当たっていた。ボード自身の「＋」で追加した予定は
普通のGoogleネイティブID形式になるため、この問題は起きなかった。

**修正:** [gas/Code.gs](gas/Code.gs) の `findRawEvent()` に保険を追加。
直接ID引き当て(`getEventById`)が失敗したら、複合ID(`実ID@開始時刻ms`)
に含まれる開始時刻の前後だけを`getEvents()`で素直に走査し、`getId()`が
一致するものを探すフォールバックを追加した。`getEvents()`で取れる
Eventオブジェクト自体は普通に`deleteEvent()`等ができるので、
「直接IDで引き直す」操作だけを避ければよい。

**検証:** ユーザーの実際の「歯科受診」予定（このID形式だった）に対し、
本番URLへ実際にupdate(member変更)→反映確認→元の状態(member:'shared')
への復元、まで実施して動作を確認した。修正前は同じ操作で確実に
再現することも先に確認済み。

**教訓:** 「＋で追加した予定の編集は動くのに、外部由来の予定だけ編集
できない」という差が出たら、まずイベントIDの形式を疑う
（ハイフン区切りの大文字ID = 要注意）。

## Apps Scriptエディタへの流し込み：clipboard越しの貼り付けは自動化からは使えない（2026-09-12）

**症状:** GASのコードをブラウザ自動化（claude-in-chrome）経由で
Apps Scriptエディタに反映しようとしたとき、`pbcopy`でクリップボードに
入れて`cmd+v`で貼り付ける手順が効かなくなっていた
（`pbcopy`後に`pbpaste`しても空、貼り付けても中身が変わらない）。
`navigator.clipboard.writeText()`/`readText()`をページ内JSで呼んでも
権限プロンプトが解決されないまま45秒でタイムアウトする
（自動化環境にはプロンプトに答えるユーザーがいないため）。

**回避策:** OSクリップボードを介さず、`ClipboardEvent('paste', ...)`を
`DataTransfer`に載せて`document.activeElement`へ直接ディスパッチする方法
に切り替えた。Monaco（Apps Scriptエディタの中身）はpasteイベントを
専用ハンドラで処理しており、これは効いた
（`dispatchEvent()`の戻り値が`false`になるのは正常——
`cancelable`なイベントをMonaco側が`preventDefault()`して自前で処理した印）。
**逆に、キー入力を1文字ずつ模したtype操作で複数行コードを流し込むのは
危険。** 自動インデント＋自動閉じ括弧が二重に効いて、レベルが下るごとに
インデントが増殖し、閉じ括弧が余分に挿入される（実験で確認済み）。

もう1つ罠だったのが、貼り付け内容（日本語コメントを含む約13.6KBの
ソース）を毎回ツール呼び出しの引数として手で書き起こすと、
まれに写し間違いが起きること（実際に一度`atob()`が
`InvalidCharacterError`で失敗した）。ローカルHTTPサーバー経由で
ブラウザにfetchさせて回避しようとしたが、**この自動化ブラウザは
localhostへのfetchも権限待ちで無期限にハングする**ため使えなかった
（`AbortController`で強制中断して初めて判明した）。
最終的には、貼り付け前に文字数とチェックサム（文字コード合計の
mod）をJS側で検算し、元ファイル（bashで計算した値）と一致しない場合は
`throw`して本番に書き込ませない、という自己検証ステップを入れることで
安全に実施した。**この手のGASコード流し込みでは、機械的な検証
（長さ＋チェックサム）を通してから初めて貼り付ける、を徹底すること。**

## 月表示：詳細パネルの出現でカレンダーが動く（2026-09-12 修正）

**症状:** 月表示で日付をタップして詳細パネルを開くたびに、月間グリッドの
高さが変わって見た目が「動く」ため落ち着かない。またパネル自体も、
その日の予定件数によって高さが伸び縮みしていた。

**原因:** [style.css](style.css) の `.month-detail` が `flex:0 0 auto` +
`max-height:26vh` で、①選択が無いときは`hidden`で高さ0、②選択すると
中身の件数ぶんだけ膨らむ（最大26vhまで）、という可変の高さだった。
`.month-grid` は`flex:1 1 auto`で残りの高さを埋める作りのため、
詳細パネルの高さが変わるたびにグリッド側の行の高さも連動して変わって
いた。

**修正:**
- 月表示に入った瞬間から、常にどこかの日付が選択されている状態にした
  （[app.js](app.js) `defaultMonthSelection()`：表示中の月に今日が
  あれば今日、無ければその月の1日をデフォルト選択）。月の切り替え
  （‹/›・スワイプ）でも選択を`null`にはせず、都度この関数で選び直す。
  これで詳細パネルが「出たり消えたり」すること自体が無くなった。
- `.month-detail` の高さを`24vh`固定にした（`max-height`ではなく
  `height`+`flex:0 0 24vh`）。中身は新設した`.md-list`（`flex:1 1 auto;
  overflow-y:auto`）だけがスクロールし、見出し（日付・件数・ヒント文）
  は常に上に固定表示されたままになる。
- 月間グリッド自体のセル表示（1マスに最大`MC_MAX=3`件＋あふれは
  「+N」）は変更していない。

**結果:** 月表示に入っても、日付を切り替えても、予定が0件でも
9件あっても、月間グリッドの高さは常に同じ（実測で確認）。詳細パネルの
中身だけが固定高さの枠内でスクロールする。

## 月表示の詳細パネル固定化で、実機ではグリッドが潰れた（2026-09-12 修正）

**症状:** 前項の「詳細パネルを24vh固定にする」対応をデプロイした後、
ユーザーの実機（iPhone・縦向き）のスクリーンショットで、月間グリッドの
セルが1行しか見えない/文字が潰れて判読できない状態になっていた。
このMacでのローカル確認（1624×750の横長と390×844の縦）では
気づけなかった不具合。

**原因:** 24vhという固定値は「壁掛けモニター(横長で縦方向にかなり
余裕がある)」を基準に決めていた。手元のiPhone(縦向き)は横幅に対して
縦の余白がそもそも少なく、時計・ツールバー・曜日行・6段のグリッドに
加えて24vh(画面の1/4)を詳細パネルに割くと、グリッド側に残る高さが
足りず、1マスに3件+"+N"を収める分の高さが確保できなくなっていた。

**修正:** [style.css](style.css) に `@media (max-width: 820px)` を追加し、
幅の狭い機種（＝手元のiPhoneなど。閾値はapp.jsの`MC_NARROW_PX`と同じ
820pxに揃えた）でだけ`.month-detail`の高さを24vh→14vhに縮小した。
壁掛けモニターのような広い画面はそのまま24vhを維持。

**教訓:** vh固定値は「幅は違うが縦横比が近い画面」でしか検証できて
いなかった。今回のように壁掛け(横長)と手元(縦長)で縦方向の余裕が
大きく異なるケースは、必ず両方の実機相当サイズで見た目を確認すること
（[README.md](README.md)にある確認手順の812×375 / 1624×750に加えて、
縦向きの390×844や428×926でも見ること）。

## 予定の保存・削除中に「閉じていいか分からない」（2026-09-12 修正）

**症状:** 予定を更新/削除すると「更新しています…」「削除しています…」
と表示されるが、追加・更新・削除ボタンだけが無効化されていて「閉じる」
ボタンは押せるままだった。処理が終わると自動でモーダルが閉じる作りに
なっているが、その説明が画面に無く、ユーザーが「このタイミングで
閉じていいのか、待たないといけないのか」判断できなかった。

**修正:** [app.js](app.js) の`submitAddEvent()`/`deleteCurrentEvent()`で、
通信中は「追加する/更新する/削除する」ボタンと同様に「閉じる」ボタンも
`disabled`にするようにした（`STATE.savingEvent`フラグを追加し、背景
タップで閉じる処理もこのフラグでガード）。メッセージも
「更新しています…このまま少しお待ちください」のように、待てばよい
ことが分かる文言にした。処理が終われば（成功時は自動で閉じる、
失敗時はエラーメッセージが出て）ボタンは全て元に戻る。
[style.css](style.css)に`.btn:disabled`（半透明表示）が無かったので
併せて追加した。

## 月間グリッド："+N"を専用の行にしていたのが手狭（2026-09-12）

**要望:** 1マスに3件まで表示し、あふれた分は「+N」で示す仕組み自体は
そのままに、この「+N」のためだけに予定と同じ高さの行をまるごと
1つ使うのがもったいない。日付の隣に右詰めで出せばスペースを有効に
使えるのでは、との提案。

**対応:** [app.js](app.js) `renderMonthView()` で、`.mc-more`（+N）を
予定一覧の下から日付の行に移動。`.mc-num`（日付）を`.mc-num-row`という
flexの行で包み、`justify-content:space-between`で日付=左・+N=右に
配置した（[style.css](style.css)）。1マスに表示する予定数の上限
（`MC_MAX = 3`）自体は変更していない。

## 月間グリッドのマス目に余白が残る（2026-09-12 修正）

**指摘:** 3件表示できるように高さを確保しているが、予定が0〜2件しか
無い日のマスにはその分の空白が余っている。この余白を削って、
下の詳細パネル（1日の予定）をもっと大きく表示できないか。

**原因:** [style.css](style.css) の `.month-grid` が
`grid-auto-rows: 1fr` で、6行を「残りの高さを均等に6等分」する
作りだった。詳細パネルを固定高さにした（前々項）ことで残りの高さは
一定になったが、それを機械的に6等分していたため、3件も0件の日も
同じ行の高さになり、少ない日には無駄な余白ができていた。

**修正:** 行の高さを「3件表示するのに実際必要な高さ」だけの固定px値
にした。[app.js](app.js) `measureMonthCellHeight()` で、画面外に
同じ構造（日付行+"+N"+3件ぶんのダミー予定）のマスを一瞬だけ作って
実測し、`--mc-row-h`というCSS変数に入れる（時間軸の`--hour-h`と同じ
実測パターン）。`.month-grid`はこの固定値×6行ぶんの高さだけを使う
`flex: 0 0 auto`にし、余った分は`.month-detail`側を`flex: 1 1 auto`
にして丸ごと譲るようにした。これにより：
- グリッドの高さは常に同じ（予定の多い日があっても増えない、
  月を切り替えても変わらない＝相変わらず「動かない」）
- 詳細パネルは以前の固定vh（14vh/24vh）指定が不要になり、画面の
  余白ぶんだけ自動的に大きくなる（実測：手元のiPhone相当の幅で
  約14vh→約36%相当まで拡大）

前項の「幅820px以下で詳細パネルを14vhに縮小」というメディアクエリは
この変更で不要になったため削除した（行の高さ自体が幅を問わず
実測値になるため）。

## 未着手・検討中

- ヘリナビ側 sw.js のプレフィックスガード（要ユーザー承認・VER上げと再デプロイが必要）
- ゴミ出しなどの定型予定の扱い
